import { FC } from 'react';
import './MenuCard.css';

interface MenuCardProps{

}

const MenuCard: FC<MenuCardProps> = () => {

    return (
        <div>

        </div>
    )
}

export default MenuCard;